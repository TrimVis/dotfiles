# The Simple X Hotkey Daemon is the program used by LARBS to map most of the
# dwm-independent commands.  This file is pretty readable, so I'll let you read
# it.

# Programs
super + Return
	$TERMINAL
super + shift + Return
	$TERMINAL -e st-cd $(XCWD)
ctrl + alt + b
	$BROWSER
ctrl + alt + f
	$FILE
ctrl + alt + g
	$TERMINAL -n "floating" -e $SYSMON
ctrl + alt + c
	$TERMINAL -n "floating -e calcurse -D $home/.config/calcurse
ctrl + alt + m
	$MAIL
ctrl + alt + v
	mpv
ctrl + alt + s
	$MUSIC

# Suspend/shutdown/lock screen
super + shift + x
	prompt "Shutdown computer?" "sudo -A shutdown -h now"
super + shift + y
	prompt "Suspend computer?" "sudo -A shutdown -h now"
super + shift + BackSpace
	prompt "Reboot computer?" "sudo -A reboot"
super + shift + Escape
	prompt 'Leave Xorg?' 'killall Xorg'
super + x
	screenlock

# Screenshot
Print
	scrot pic-full-$(date '+%y%m%d-%H%M-%S').png
shift + Print
	scrot --select pic-sel-$(date '+%y%m%d-%H%M-%S').png
ctrl + Print
	scrot --focused pic-focus-$(date '+%y%m%d-%H%M-%S').png

# Audiokeys
XF86AudioMute
	amixer sset Master toggle; pkill -RTMIN+10 $STATUSBAR
XF86Audio{Raise,Lower}Volume
	amixer sset Master 5%{+,-}; pkill -RTMIN+10 $STATUSBAR

XF86PowerOff
	prompt "Shutdown computer?" "sudo -A shutdown -h now"
XF86Calculator
	$TERMINAL -e bc
XF86Sleep
	prompt 'Hibernate computer?' 'sudo -A zzz'

# Function keys
# Change display
mod + p
	displayselect
# Mount a USB drive or Android device
super + {F9,F10}
	{dmenumount,dmenuumount}
