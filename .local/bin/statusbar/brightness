#!/usr/bin/env bash

TOUT=$(xset q | grep timeout | awk '{ print $2 }')

case $BLOCK_BUTTON in
    1) if [ "$TOUT" = "0" ]
    then
        xset s on
    else
        xset s off
    fi
esac

light -G | awk -F'.' '{ print $1 }'

if [ "$TOUT" = "0" ]
then
    echo " "
    echo "#0000AF"
fi
