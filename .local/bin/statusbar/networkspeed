#!/bin/sh

IFACE="wlp1s0"
UPNDOWN=$(sar -n DEV 1 3 | grep $IFACE | tail -n1 | awk '{print $5","$6}')

RECV=$(echo "$UPNDOWN" | awk -F, '{print $1}')
SEND=$(echo "$UPNDOWN" | awk -F, '{print $3}')

echo "⬇${RECV}kB ⬆${SEND}kB"
#echo "$UPNDOWN"
echo ""
echo "#8EE400"
